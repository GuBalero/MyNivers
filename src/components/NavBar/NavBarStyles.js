import styled from 'styled-components';

export const Logo = styled.h1`
    color: #3c00ff;
    text-shadow: 0 0 20px #3c00cc;
    letter-spacing: 2px;
    font-size: max(30px, 3vw);
    font-family: 'Permanent Marker', cursive;
`;